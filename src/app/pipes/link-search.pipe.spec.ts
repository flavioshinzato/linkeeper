import { LinkSearchPipe } from './link-search.pipe';

describe('LinkSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new LinkSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
